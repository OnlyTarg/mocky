name: mocky
description: A clean, extensible data generator for tests, demos, and mock data. Supports multiple locales (Ukrainian, US) with words, names, phone numbers, and emails.
version: 0.1.0
repository: https://github.com/onlytarg/mocky
homepage: https://github.com/onlytarg/mocky

environment:
  sdk: ^3.5.4

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
